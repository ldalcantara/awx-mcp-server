[pytest]
# Tests are run from this directory in CI (working-directory: ./tests), so the
# pytest config in server/pyproject.toml is not discovered here. Enable
# pytest-asyncio's auto mode so async tests/fixtures are handled.
asyncio_mode = auto
